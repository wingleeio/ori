---
"@wingleeio/ori-core": minor
"@wingleeio/ori-react": minor
---

Add a built-in to-do (task) list block with a clickable checkbox, nesting and keyboard behavior shared with the other list types, matching React rendering, and clipboard round-tripping (private JSON, checkbox HTML, and `- [ ]` / `- [x]` plain text).
